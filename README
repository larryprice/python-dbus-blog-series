This is a companion repository for my blog post series on python d-bus services at https://larry-price.com

Run the service with `./service &`. Now you can run `./client [n]`, where `n` is an integer. To kill the service, make sure to record the pid displayed after it starts in the background.
